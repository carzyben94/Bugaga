services:
  - type: web
    name: bugaga-bot
    runtime: python
    buildCommand: pip install -r requirements.txt && pip install playwright && python -m playwright install chromium --only-shell
    startCommand: python bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0


